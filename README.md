# Laptop Tracking Web App

Simple single-page web app for laptop tracking with:
- Login system (admin + agent)
- Tracking for location, usage time, inventory asset ID
- Admin panel (admin-only controls)

## Run
Open `index.html` directly in browser.

## Demo credentials
- `admin / admin123`
- `agent / agent123`
